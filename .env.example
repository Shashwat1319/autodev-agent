# GitHub API Token — required for profile analysis
# Create a classic personal access token:
# https://github.com/settings/tokens
GITHUB_TOKEN=

# Optional: Unauthenticated requests are rate-limited to 60/hr
# With a token: 5,000 requests/hr
# Recommended: create a token with no scopes (public access only)
